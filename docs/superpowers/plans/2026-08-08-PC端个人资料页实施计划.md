# PC 端个人资料页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 PC 用户端增加可从侧栏账户区域进入的只读个人资料页，并通过受 JWT 保护的接口加载当前用户资料。

**Architecture:** 前端新增独立 `ProfilePage.vue`，只依赖认证状态和新增 `GET /api/users/me`。后端从 JWT 中间件注入的 `userID` 查询用户，再转换为显式的安全响应类型，绝不直接返回数据库用户模型。

**Tech Stack:** Vue 3、Vue Router、Vite、Gin、GORM、Node 内建测试、Go 标准测试。

---

## 文件结构

- `src/utils/profile.js`：资料字段和创建时间的纯数据转换。
- `src/utils/profile.test.js`：手机号隐藏、日期格式化的 Node 测试。
- `src/views/ProfilePage.vue`：PC 端只读资料页面。
- `src/components/SiteHeader.vue`：账户区域跳转入口。
- `src/router/index.js`：`/profile` 路由和登录守卫。
- `src/api/auth.js`：当前用户资料请求。
- `D:/go代码/quanzhan/blog/models/vo/user.go`：可公开的用户资料响应类型。
- `D:/go代码/quanzhan/blog/internal/service/service.go`：资料查询与转换。
- `D:/go代码/quanzhan/blog/internal/handler/handler.go`：`UsersMe` 处理器。
- `D:/go代码/quanzhan/blog/internal/router/router.go`：受保护的 `GET /users/me`。

### Task 1: 前端资料数据工具

**Files:**
- Create: `src/utils/profile.js`
- Create: `src/utils/profile.test.js`

- [ ] **Step 1: 写失败测试**

```js
import assert from 'node:assert/strict'
import test from 'node:test'
import { profileRows, formatCreatedAt } from './profile.js'

test('手机号为空时不创建手机号行', () => {
  const rows = profileRows({ email: 'a@example.com', nickname: '阿明', phone: null, created_at: '2026-08-08T03:04:05Z' })
  assert.deepEqual(rows.map((row) => row.label), ['邮箱', '昵称', '账号创建时间'])
})

test('手机号存在时展示手机号', () => {
  const rows = profileRows({ email: 'a@example.com', nickname: '阿明', phone: '13800000000', created_at: '2026-08-08T03:04:05Z' })
  assert.equal(rows.at(-1).label, '手机号')
  assert.equal(rows.at(-1).value, '13800000000')
})

test('ISO 时间格式化为本地日期时间', () => {
  assert.match(formatCreatedAt('2026-08-08T03:04:05Z'), /^2026-08-08 /)
})
```

- [ ] **Step 2: 运行并确认失败**

Run: `node --test src/utils/profile.test.js`

Expected: `ERR_MODULE_NOT_FOUND`，因为 `profile.js` 尚未创建。

- [ ] **Step 3: 实现最小数据工具**

```js
export function formatCreatedAt(value) {
  if (!value) return '未知'
  const date = new Date(value)
  if (Number.isNaN(date.getTime())) return '未知'
  const pad = (number) => String(number).padStart(2, '0')
  return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())} ${pad(date.getHours())}:${pad(date.getMinutes())}`
}

export function profileRows(profile) {
  const rows = [
    { label: '邮箱', value: profile.email || '未知' },
    { label: '昵称', value: profile.nickname || profile.email || '未知' },
    { label: '账号创建时间', value: formatCreatedAt(profile.created_at) },
  ]
  if (profile.phone) rows.push({ label: '手机号', value: profile.phone })
  return rows
}
```

- [ ] **Step 4: 运行通过测试**

Run: `node --test src/utils/profile.test.js`

Expected: 3 个子测试全部 `ok`。

- [ ] **Step 5: 提交**

Run: `git add src/utils/profile.js src/utils/profile.test.js; git commit -m "test: add profile data helpers"`

### Task 2: PC 页面、路由与账户入口

**Files:**
- Create: `src/views/ProfilePage.vue`
- Modify: `src/router/index.js`
- Modify: `src/components/SiteHeader.vue`
- Modify: `src/api/auth.js`

- [ ] **Step 1: 先注册不存在页面的路由，建立失败构建**

```js
{ path: '/profile', name: 'profile', component: () => import('../views/ProfilePage.vue'), meta: { requiresUser: true } },
```

Run: `npm run build`

Expected: 构建失败并提示 `../views/ProfilePage.vue` 无法解析。

- [ ] **Step 2: 增加资料请求和用户路由守卫**

在 `src/api/auth.js` 添加：

```js
export function getCurrentUser() {
  return request('GET', '/api/users/me')
}
```

在 `src/router/index.js` 的 `createRouter` 后添加：

```js
router.beforeEach((to) => {
  if (!to.meta.requiresUser) return true
  if (localStorage.getItem('blog-user')) return true
  window.dispatchEvent(new Event('show-auth-modal'))
  return { name: 'home' }
})
```

- [ ] **Step 3: 实现 `ProfilePage.vue`**

创建只读资料页：使用 `useAuth()` 的本地用户作为初始 `profile`，用 `onMounted` 调用 `getCurrentUser()` 更新资料；从 `../utils/profile.js` 导入 `profileRows`；用 `computed` 生成头像首字母和字段行。

模板必须包含：

```vue
<section class="profile-page">
  <router-link class="back-link" to="/">← 返回首页</router-link>
  <header class="profile-hero">
    <span class="profile-avatar">{{ initial }}</span>
    <div><h1>个人资料</h1><p>{{ profile.nickname || profile.email }}</p></div>
  </header>
  <p v-if="loading" class="profile-status">正在加载资料...</p>
  <p v-else-if="error" class="profile-status profile-error">资料加载失败</p>
  <dl class="profile-list"><template v-for="row in rows" :key="row.label"><dt>{{ row.label }}</dt><dd>{{ row.value }}</dd></template></dl>
</section>
```

样式最大宽度为 `560px`，用 `--border-light` 分隔行，复用现有颜色变量；本文件不添加移动端媒体查询。

- [ ] **Step 4: 使侧栏账户区域跳转资料页**

在 `SiteHeader.vue` 导入 `useRouter`，并添加：

```js
const router = useRouter()
function goToProfile() {
  dropdownOpen.value = false
  router.push({ name: 'profile' })
}
```

将已登录 `.btn-user` 的点击处理改为 `@click="goToProfile"`，加上 `title="查看个人资料"`；保留下拉菜单和退出登录。仅对 `.btn-user` 增加 `text-align: left`，不得改变侧栏宽度与高度。

- [ ] **Step 5: 验证与提交**

Run: `node --test src/utils/profile.test.js; npm run build`

Expected: 3 个 Node 测试通过，Vite 成功生成 `dist`。

手工验证：登录后点击账户区域进入 `/profile`；清除 `blog-user` 后直接访问 `/profile`，会回首页并弹出登录框；手机号为空时不显示该行。

Run: `git add src/api/auth.js src/components/SiteHeader.vue src/router/index.js src/utils/profile.js src/utils/profile.test.js src/views/ProfilePage.vue; git commit -m "feat: add PC user profile page"`

### Task 3: 后端安全资料模型与服务方法

**Files:**
- Create: `D:/go代码/quanzhan/blog/models/vo/user.go`
- Create: `D:/go代码/quanzhan/blog/internal/service/user_profile_test.go`
- Modify: `D:/go代码/quanzhan/blog/internal/service/service.go`

- [ ] **Step 1: 写失败测试，锁定可公开字段**

```go
func TestUserProfileFromUser(t *testing.T) {
    phone := "13800000000"
    user := models.User{ID: 7, Email: "a@example.com", Nickname: "阿明", Phone: &phone, Password: "secret", WechatOpenID: "openid", CreatedAt: time.Date(2026, 8, 8, 3, 4, 5, 0, time.UTC)}
    profile := UserProfileFromUser(user)
    if profile.ID != 7 || profile.Email != "a@example.com" || profile.Nickname != "阿明" || profile.Phone == nil || *profile.Phone != phone || !profile.CreatedAt.Equal(user.CreatedAt) {
        t.Fatalf("unexpected profile: %#v", profile)
    }
}

func TestUserProfileFromUserKeepsNilPhone(t *testing.T) {
    if profile := UserProfileFromUser(models.User{}); profile.Phone != nil {
        t.Fatalf("expected nil phone")
    }
}
```

导入 `blog/models`、`testing`、`time`。

- [ ] **Step 2: 运行并确认失败**

Run: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./internal/service -run UserProfile -count=1`

Expected: 编译失败，指出 `UserProfileFromUser` 未定义。

- [ ] **Step 3: 创建响应类型和服务转换**

创建 `models/vo/user.go`：

```go
package vo

import "time"

type UserProfile struct {
    ID        uint64    `json:"id"`
    Email     string    `json:"email"`
    Nickname  string    `json:"nickname"`
    Phone     *string   `json:"phone"`
    CreatedAt time.Time `json:"created_at"`
}
```

在 `service.go` 导入 `blog/models/vo`，并添加：

```go
func UserProfileFromUser(user models.User) vo.UserProfile {
    return vo.UserProfile{ID: user.ID, Email: user.Email, Nickname: user.Nickname, Phone: user.Phone, CreatedAt: user.CreatedAt}
}

func (s *Service) CurrentUserProfile(userID uint64) (vo.UserProfile, error) {
    user, err := s.repo.GetUserByID(userID)
    if err != nil { return vo.UserProfile{}, err }
    return UserProfileFromUser(user), nil
}
```

- [ ] **Step 4: 验证与提交**

Run: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./internal/service -run UserProfile -count=1`

Expected: 2 个测试通过。

Run: `git -C D:/go代码/quanzhan/blog add models/vo/user.go internal/service/service.go internal/service/user_profile_test.go; git -C D:/go代码/quanzhan/blog commit -m "feat: add current user profile service"`

### Task 4: JWT 保护的资料接口

**Files:**
- Create: `D:/go代码/quanzhan/blog/internal/handler/user_profile_test.go`
- Modify: `D:/go代码/quanzhan/blog/internal/handler/handler.go`
- Modify: `D:/go代码/quanzhan/blog/internal/router/router.go`

- [ ] **Step 1: 写失败测试，拒绝无令牌访问**

```go
func TestCurrentUserProfileRejectsMissingToken(t *testing.T) {
    gin.SetMode(gin.TestMode)
    h := New(service.New(nil, nil, nil))
    r := gin.New()
    r.GET("/api/users/me", middleware.JWTAuth(), h.User.UsersMe)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, httptest.NewRequest(http.MethodGet, "/api/users/me", nil))
    if w.Code != http.StatusOK || !strings.Contains(w.Body.String(), `"code":1005`) {
        t.Fatalf("expected unauthorized response, got %d: %s", w.Code, w.Body.String())
    }
}
```

若 `code.Unauthorized` 并非 `1005`，使用 `strconv.Itoa(int(code.Unauthorized))` 构造断言，避免硬编码错误业务码。

- [ ] **Step 2: 运行并确认当前空处理器不能返回资料**

Run: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./internal/handler -run CurrentUserProfile -count=1`

Expected: 无令牌测试通过中间件，但当前受保护路由尚未注册，完整路由测试无法覆盖目标接口。

- [ ] **Step 3: 实现处理器并移动路由**

将 `UsersMe` 替换为：

```go
func (h *UserHandler) UsersMe(c *gin.Context) {
    userID, ok := currentUserID(c)
    if !ok { response.ErrWithMsg(code.Unauthorized, c); return }
    profile, err := h.svc.CurrentUserProfile(userID)
    if err != nil {
        if errors.Is(err, gorm.ErrRecordNotFound) { response.ErrWithMsg(code.ErrUserNotFound, c); return }
        zap.L().Error("UsersMe:" + err.Error())
        response.ErrWithMsg(code.InternalError, c)
        return
    }
    response.SuccessWithData(profile, c)
}
```

删除公开组中的 `public.POST("/users/me", h.User.UsersMe)`，在 `apiAuth` 组增加：

```go
apiAuth.GET("/users/me", h.User.UsersMe)
```

不得从路径、查询或请求体接收用户 ID。

- [ ] **Step 4: 完整验证与提交**

Run: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./...; go build ./...`

Expected: Go 测试和构建全部通过。

Run: `git -C D:/go代码/quanzhan/blog add internal/handler/handler.go internal/handler/user_profile_test.go internal/router/router.go; git -C D:/go代码/quanzhan/blog commit -m "feat: expose current user profile endpoint"`

### Task 5: 浏览器验收

**Files:** 无，只验证。

- [ ] **Step 1: 启动服务**

在 `D:/go代码/quanzhan/blog` 运行 `go run main.go`，在 `D:/go代码/quanzhan/vue6122` 运行 `npm run dev -- --host 127.0.0.1`。

- [ ] **Step 2: 验收安全与页面状态**

确认登录用户的页面显示邮箱、昵称、账号创建时间，且有手机号才显示手机号。无 Authorization 调用 `GET /api/users/me` 必须返回未认证业务响应。成功响应只能含 `id`、`email`、`nickname`、`phone`、`created_at`，不含 `password`、`wechat_open_id`、`status`。

- [ ] **Step 3: 最终命令验证**

Run: `node --test src/utils/profile.test.js; npm run build`（前端）以及 `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./...; go build ./...`（后端）。

Expected: 所有测试与构建通过。

