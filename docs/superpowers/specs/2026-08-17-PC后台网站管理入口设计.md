# PC 后台网站管理入口设计

## 目标

在后台左侧导航增加“网站管理”入口，并提供可独立扩展的空白页面。

## 范围

- 仅修改 PC 前端后台管理区域。
- 新增 `/admin/site` 路由和对应页面。
- 左侧导航将入口放在“用户管理”之后，沿用现有导航高亮样式。
- 页面只展示“网站管理”的空状态，不接入接口、不新增配置字段、不修改后端或微信小程序。

## 验证方式

- 从左侧菜单可进入 `/admin/site`。
- 进入后“网站管理”菜单处于高亮状态。
- 执行 `npm run build` 成功。
